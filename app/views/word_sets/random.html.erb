<%= content_for(:title, @word_set.words_ordered_by_current_query.map(&:display).join(", ")) %>

<div class="container">
  <div class="row">
    <div class="col-sm-9">
      <%= content_tag page_h, "A random homophone set" %>
    </div>
    <div class="col-sm-3">
      <%= content_tag page_h, link_to("Click for another random homophone!", random_homophone_path, class: "btn btn-info btn-xs-full") %>
    </div>
  </div>

  <div class="card-set-container">
    <%= render partial: "card" %>

    <%= render partial: "ads/card" %>
  </div>

  <%= render 'layouts/more_homophones' %>
</div>
