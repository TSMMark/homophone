<% content_for(:title, "About Homophones") %>

<% content_for(:desc,
   "Why do some words sound the same? What are homophones? Learn more at www.Homophone.com. " +
   default_page_description) %>

<%= render partial: 'sections/random_homophones' %>

<%= render partial: 'sections/about_homophones' %>

<%= render partial: 'sections/more_info' %>

<%= render "ads/horizontal" %>

<%= render partial: 'sections/why_homophones_exist' %>

<%= render "ads/horizontal" %>

<%= render partial: 'sections/about_author' %>
